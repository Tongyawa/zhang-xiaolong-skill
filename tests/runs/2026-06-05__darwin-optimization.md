# 2026-06-05 Darwin 优化记录

## 目标

按 Darwin Skill 9 维 Rubric 优化 `SKILL.md`，只处理本轮最低分短板：失败模式编码与检查点设计。

## 测试 Prompt

详见仓库根目录 `test-prompts.json`，覆盖：

- 知识管理产品连续签到、积分、排行榜提高留存。
- 学习 App 用 AI 推荐信息流增加时长。
- 平台大客户要求特殊接口。

## 基线评估

独立 subagent 只读评估，维度 8 为 dry-run。

- 总分：78.8 / 100。
- 最低维度：检查点设计 2 / 10。
- 次低维度：失败模式编码 5 / 10。
- 主要问题：缺少显性 `CHECKPOINT / STOP`，缺少 `如果 X → Y` 的失败分支。

## 改动

- `SKILL.md`：在回答工作流后新增 `🔴 CHECKPOINT：先停下的红灯`。
- `SKILL.md`：新增事实、目标、平台特权、用户伤害 4 类红灯与 9 条分支动作。
- `SKILL.md`：新增输出骨架，稳定输出结论、用户任务、破坏点、替代做法、验证方式、失效条件。
- `scripts/quality_check.py`：新增红灯检查点、失败分支、输出骨架的静态检查。

## 复评

独立 subagent 只读复评，维度 8 为 dry-run。

- 总分：83.2 / 100。
- 失败模式编码：5 → 8。
- 检查点设计：2 → 9。
- 实测表现：9，三个场景均稳定导向用户任务、低打扰、公平规则、非指标崇拜。

## 横评

独立 subagent 对比基线版与优化版，结论：保留优化版。

- 提升：失败模式、显性检查点、输出稳定性、可执行性。
- 轻微风险：输出骨架可能让角色回答略模板化；目标红灯如果触发过宽，可能在普通增长/AI 问题上多问澄清。
- 已处理：目标红灯改为“缺少关键目标且无法判断用户任务”才追问；已有具体产品、功能和指标时先给取舍，再标注假设。

## 验证

命令：

```powershell
python scripts\quality_check.py
```

结果：通过。

新增检查项：

- 红灯检查点：4 类。
- 失败分支：9 条。
- 输出骨架：6 步。

## 未覆盖

- 没有真实运行 full_test 对话，只做 dry-run。
- 没有联网更新 2026 年后公开事实。
- 没有继续压缩时间线和身份材料；复评指出其对运行指令有轻微稀释，但本轮按最小优化原则不处理。
